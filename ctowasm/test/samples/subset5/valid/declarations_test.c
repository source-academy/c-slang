// declaration of type
typedef int x;

// first declaration of a struct tag
struct A {
  int x;
} a;

enum E {
  t,
  y = 20,
} e;

// declaration of function
int f(x, x y, int x);

int main() {
  // test static declaration
  static int a;
  
  typedef int * T;
  T pp = &a;
  {
    int y;
    y * y;
  }
  x y;
  x *x = &y;
}